import QtQuick
import QtQuick.Window
import Felgo

// colors
// #774422 - hair
// #eebb99 - skin
// #2e4b8d - eye
// #bb8866 - nose

Window {
    id: root 
    visible: true

    width: 400
    height: 400

    Rectangle {
        id: steve
        height: 50
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422"
        Pixel { pixelColor: "#774422" 
        } } } } } } } } 

        PixelRow {
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422"
            Pixel { pixelColor: "#774422" 
            } } } } } } } } 

            PixelRow {
                Pixel { pixelColor: "#774422"
                Pixel { pixelColor: "#eebb99"
                Pixel { pixelColor: "#eebb99"
                Pixel { pixelColor: "#eebb99"
                Pixel { pixelColor: "#eebb99"
                Pixel { pixelColor: "#eebb99"
                Pixel { pixelColor: "#eebb99"
                Pixel { pixelColor: "#774422" 
                } } } } } } } } 

                PixelRow {
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99"
                    Pixel { pixelColor: "#eebb99" 
                    } } } } } } } } 

                    PixelRow {
                        Pixel { pixelColor: "#eebb99"
                        Pixel { pixelColor: "#ffffff"
                        Pixel { pixelColor: "#2e4b8d"
                        Pixel { pixelColor: "#eebb99"
                        Pixel { pixelColor: "#eebb99"
                        Pixel { pixelColor: "#2e4b8d"
                        Pixel { pixelColor: "#ffffff"
                        Pixel { pixelColor: "#eebb99" 
                        } } } } } } } } 

                        PixelRow {
                            Pixel { pixelColor: "#eebb99"
                            Pixel { pixelColor: "#eebb99"
                            Pixel { pixelColor: "#eebb99"
                            Pixel { pixelColor: "#bb8866"
                            Pixel { pixelColor: "#bb8866"
                            Pixel { pixelColor: "#eebb99"
                            Pixel { pixelColor: "#eebb99"
                            Pixel { pixelColor: "#eebb99" 
                            } } } } } } } } 

                            PixelRow {
                                Pixel { pixelColor: "#eebb99"
                                Pixel { pixelColor: "#eebb99"
                                Pixel { pixelColor: "#774422"
                                Pixel { pixelColor: "#eebb99"
                                Pixel { pixelColor: "#eebb99"
                                Pixel { pixelColor: "#774422"
                                Pixel { pixelColor: "#eebb99"
                                Pixel { pixelColor: "#eebb99" 
                                } } } } } } } } 

                                PixelRow {
                                    Pixel { pixelColor: "#eebb99"
                                    Pixel { pixelColor: "#eebb99"
                                    Pixel { pixelColor: "#774422"
                                    Pixel { pixelColor: "#774422"
                                    Pixel { pixelColor: "#774422"
                                    Pixel { pixelColor: "#774422"
                                    Pixel { pixelColor: "#eebb99"
                                    Pixel { pixelColor: "#eebb99" 
                                    } } } } } } } } 
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}